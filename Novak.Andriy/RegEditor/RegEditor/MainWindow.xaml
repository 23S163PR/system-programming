<Window x:Class="RegEditor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:self="clr-namespace:RegEditor"
        Title="MainWindow" Height="350" Width="525">
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition/>
			<ColumnDefinition/>
		</Grid.ColumnDefinitions>
		<TreeView Name="RegistryKeys" Grid.Column="0" VerticalAlignment="Top" 
				  SelectedItemChanged="RegistryKeys_OnSelectedItemChanged">    
			<TreeView.ItemTemplate>
				<HierarchicalDataTemplate  DataType="{x:Type self:TreeItem}" ItemsSource="{Binding Items}">
					<TextBlock Text="{Binding Title}"/>
				</HierarchicalDataTemplate>
			</TreeView.ItemTemplate>
		</TreeView>
		<DataGrid Grid.Column="1" Name="InfoDataGrid" 
                  HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
			<DataGridTextColumn Header="Name"/>
		</DataGrid>
	</Grid>
</Window>
