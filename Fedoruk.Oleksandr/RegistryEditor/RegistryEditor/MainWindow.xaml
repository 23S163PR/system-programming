<Window x:Class="RegistryEditor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:RegistryEditor.Classes"
        Title="MainWindow" Height="350" Width="625">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <TreeView Name="treeView" Grid.Column="0" TreeViewItem.Expanded="treeView_Expanded">
            <TreeView.Resources>
                <HierarchicalDataTemplate ItemsSource="{Binding Path=Items}" DataType="{x:Type local:TreeViewNode}">
                    <TextBlock Text="{Binding Path=Name}"></TextBlock>
                </HierarchicalDataTemplate>
            </TreeView.Resources>
        </TreeView>
        <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch" />
        <DataGrid Grid.Column="2">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name"  Width="*" />
                <DataGridTextColumn Header="Type"  Width="*" />
                <DataGridTextColumn Header="Value" Width="2*" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
